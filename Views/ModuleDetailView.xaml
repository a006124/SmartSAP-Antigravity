<UserControl x:Class="Views.ModuleDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:SmartSAP.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <Button Command="{Binding GoBackCommand}" Background="Transparent" BorderThickness="0" Margin="0,0,15,0">
                <TextBlock Text="" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="{StaticResource TextPrimaryBrush}"/>
            </Button>
            <StackPanel>
                <TextBlock Text="{Binding ModuleTitle}" Style="{StaticResource Header1}"/>
                <TextBlock Text="Exécutez les étapes ci-dessous pour finaliser l'opération SAP." Style="{StaticResource SubHeader}"/>
            </StackPanel>
        </DockPanel>

        <!-- Control Bar -->
        <Border Grid.Row="1" Style="{StaticResource ModuleCard}" Padding="10" Margin="0,0,0,30">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding RunWorkflowCommand}" Background="#10B981" Foreground="White" Padding="20,5" BorderThickness="0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="" FontFamily="Segoe MDL2 Assets" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Run Workflow" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
                <Separator Width="1" Height="24" Margin="15,0" Background="{StaticResource BorderBrush}"/>
                <TextBlock Text="Statut : Prêt" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock Text="   |   Dernière exécution : Jamais" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>
        </Border>

        <!-- Workflow Steps -->
        <ItemsControl Grid.Row="2" ItemsSource="{Binding Steps}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Border Width="200" Height="220" Style="{StaticResource ModuleCard}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Width="40" Height="40" CornerRadius="8" Background="{StaticResource BluePastelBrush}" HorizontalAlignment="Left" Margin="0,0,0,15">
                                    <TextBlock Text="{Binding Icon}" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18" Foreground="{StaticResource AccentBlueBrush}"/>
                                </Border>

                                <StackPanel Grid.Row="1">
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                                </StackPanel>

                                <Border Grid.Row="2" Background="{StaticResource WindowBackgroundBrush}" CornerRadius="4" Padding="5" HorizontalAlignment="Stretch">
                                    <TextBlock Text="{Binding Status}" HorizontalAlignment="Center" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
                                </Border>
                            </Grid>
                        </Border>
                        <!-- Connector -->
                        <TextBlock Text="→" FontFamily="Segoe UI" FontSize="24" VerticalAlignment="Center" Margin="5" Foreground="{StaticResource BorderBrush}" 
                                   Visibility="{Binding RelativeSource={RelativeSource PreviousData}, Converter={StaticResource ResourceKey=NullToVisibilityConverter}}"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Log Section -->
        <Border Grid.Row="3" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,1,1,0" CornerRadius="8,8,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <Border Background="{StaticResource WindowBackgroundBrush}" CornerRadius="8,8,0,0" Padding="10">
                    <TextBlock Text="Execution Logs" FontWeight="Bold" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                </Border>
                
                <ScrollViewer Grid.Row="1" Margin="10">
                    <ItemsControl ItemsSource="{Binding Logs}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Margin="0,2">
                                    <Run Text="{Binding Timestamp}" Foreground="#94A3B8" FontFamily="Consolas"/>
                                    <Run Text=" ["/><Run Text="{Binding Type}" FontWeight="Bold"/><Run Text="] "/>
                                    <Run Text="{Binding Message}" Foreground="{StaticResource TextPrimaryBrush}"/>
                                </TextBlock>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
